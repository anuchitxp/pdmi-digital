Save the AlmaLinux 9 deployment runbook as `DEPLOY.md` in the repo root, covering: Node 22 install via NodeSource (dnf), clone + `npm ci` + `prisma migrate dev` + `npm run build`, pm2 startup with boot persistence, SELinux note, firewalld rich-rule restricting port 3000 to the admin IP plus the matching Vultr firewall-group rule, the no-auth/PDPA caution (fake seed data only, IP-restricted), verification step, and the backup cron entry. One file write, no other changes.