No file changes required — this was a usage question. Recommended setup: run Zcode on the host as today (it shares $HOME with the distrobox, so ~/.zcode config and the project files are the same), and execute AlmaLinux 9 commands via `distrobox enter alma9 -- <cmd>` for builds/tests/migrations. Alternative: install Node 22 inside the box and run the zcode CLI there, reusing the shared ~/.zcode config.